Desktop UI archive (PySide6)

Snapshotted when LocalFun moved to the FastAPI web app.

- `ui/` — former `localfun/ui` package
- `app.py` / `gui.py` — former desktop entry points

Do not import this package from the live app.
