# Core — works on PC and Termux (web UI, library, peer sync, MangaDex)
# No img2pdf/pikepdf (needs qpdf; fails on Termux). No Playwright/av.
requests>=2.32.0
Pillow>=10.0.0
cryptography>=42.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.0
python-multipart>=0.0.9
httpx>=0.27.0
tzdata>=2024.1
